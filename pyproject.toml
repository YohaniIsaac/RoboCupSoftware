[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "robocup-software"
version = "0.1.0"
dependencies = [
    # desde robot_soccer/requirements.txt
]

[tool.setuptools.packages.find]
where = ["."]
include = ["robot_soccer*"]
